#include <stdio.h>
#include <stdlib.h>

void main()
{
    int i=1, sum=0;
    while (i<=10)
    {
        sum=sum+i;
        i++; // can use i=i+2 for the sum of odd nos. between 0 and 10
    }
    printf("\n\n\t\t The sum is = %d", sum);

}
00
